{
    "private": true,
    "name": "coolkidssatit",
    "workspaces": [
        "apps/*",
        "packages/*"
    ],
    "scripts": {
        "bootstrap": "turbo run bootstrap --no-cache",
        "deploy": "turbo run deploy",
        "postinstall": "patch-package"
    },
    "dependencies": {
        "dotenv-mono": "^1.3.9",
        "turbo": "^1.6.3"
    }
}