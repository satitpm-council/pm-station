import type { AppConfig } from "config";

/**
 * **Global app configuration.**
 *
 * Rename this file to `app.config.ts` and fill in the values.
 *
 * For more information, see the documentation of `AppConfig` in `packages/config/types.ts`.
 */
const config: AppConfig = {};

export default config;
