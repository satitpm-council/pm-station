/**
 * We currently want to show only the search form, which is defined as a persistent layout component.
 * However, we still need to return null so that Next.js will render the page.
 *
 * If we would like to add some content to this page, we can do so by adding it here.
 */
export default function Page() {
  return null;
}
